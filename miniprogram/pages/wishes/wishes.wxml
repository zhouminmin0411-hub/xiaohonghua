<view class="page-container">
  <view class="header">
    <view class="header-title">许愿瓶</view>
    <view class="flower-counter">
      <text class="flower-counter-icon">🌸</text>
      <text class="flower-counter-number">{{currentPoints}}</text>
    </view>
  </view>

  <view class="jar-section">
    <view class="jar-container {{jarAnimate ? 'jar-pop' : ''}}">
      <image class="jar-image" src="{{jarImage}}" mode="aspectFit" />
      <view class="jar-label">
        <text>本周目标 {{currentPoints}}/{{goalPoints}}</text>
      </view>
      
      <!-- 飘落花瓣动画 -->
      <view class="petal-container">
        <view 
          wx:for="{{petalCloud}}" 
          wx:key="index"
          class="petal"
          style="animation-duration: {{item.duration}}s; animation-delay: -{{item.duration * item.phase}}s;"
        >
          🌸
        </view>
      </view>
    </view>
    
    <view class="jar-tips">
      <text>收集小红花，装满许愿瓶，</text>
      <text>就可以实现一个愿望哦！</text>
    </view>
  </view>
</view>
