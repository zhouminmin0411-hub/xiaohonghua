# 微信云开发快速迁移指南 - 小红花项目

## 🎯 目标
将后端API迁移到微信云开发云函数，实现无需域名即可上线

**预计时间：** 3-4小时  
**优势：** 无需域名备案，今天就能提交审核

---

## 📊 迁移方案

### 当前架构 vs 云函数架构

| 组件 | 云托管方案 | 云函数方案 |
|-----|-----------|-----------|
| **后端** | Spring Boot (Java) | 云函数 (Node.js) |
| **数据库** | 云托管MySQL | 云开发数据库 |
| **域名** | 需要备案域名 ✗ | 不需要域名 ✓ |
| **调用方式** | HTTP API | wx.cloud.callFunction() |
| **上线时间** | 20天（等备案） | 今天 ✓ |

---

## 📋 第一步：开通云开发

### 1. 在微信开发者工具开通

1. **打开微信开发者工具**
2. **点击"云开发"按钮**（顶部工具栏）
3. **开通云开发**
   - 环境名称：`xiaohonghua-prod`
   - 点击"开通"
4. **等待开通完成**（约1-2分钟）

### 2. 获取环境ID

开通后会显示环境ID，格式如：`xiaohonghua-7gxxx`

**记录这个环境ID，后面会用到！**

---

## 📦 第二步：初始化云数据库

### 1. 设计数据库集合（Collection）

云开发使用文档数据库（类似MongoDB），需要转换MySQL表结构：

| MySQL表 | 云数据库集合 | 说明 |
|---------|------------|------|
| `user` | `users` | 用户信息 |
| `task` | `tasks` | 任务列表 |
| `reward` | `rewards` | 奖品列表 |
| `task_record` | `task_records` | 任务记录 |
| `reward_record` | `reward_records` | 兑换记录 |
| `point_history` | `point_history` | 积分历史 |
| `weekly_point_config` | `weekly_configs` | 周积分配置 |

### 2. 创建集合

在云开发控制台 → 数据库：

```javascript
// 1. 创建 users 集合
// 索引：openid（唯一）

// 2. 创建 tasks 集合
// 索引：type, is_active

// 3. 创建 rewards 集合
// 索引：cost, is_active

// 4. 创建 task_records 集合
// 索引：child_id, task_id, status

// 5. 创建 reward_records 集合
// 索引：child_id, reward_id

// 6. 创建 point_history 集合
// 索引：child_id, created_at

// 7. 创建 weekly_configs 集合
```

### 3. 初始化默认数据

在云开发控制台 → 数据库 → 导入数据，上传初始数据（我会为您准备JSON格式）

---

## ⚡ 第三步：创建云函数

### 云函数列表

需要创建以下云函数：

| 云函数名 | 功能 | 对应原API |
|---------|------|----------|
| `login` | 微信登录 | POST /api/auth/wechat-login |
| `getTasks` | 获取任务列表 | GET /api/tasks |
| `createTask` | 创建任务 | POST /api/tasks |
| `updateTask` | 更新任务 | PUT /api/tasks/:id |
| `deleteTask` | 删除任务 | DELETE /api/tasks/:id |
| `receiveTask` | 领取任务 | POST /api/task-records/receive |
| `completeTask` | 完成任务 | POST /api/task-records/complete |
| `getTaskRecords` | 查询任务记录 | GET /api/task-records |
| `getCurrentPoints` | 获取当前积分 | GET /api/points/current |
| `getPointHistory` | 获取积分历史 | GET /api/points/history |
| `adjustPoints` | 调整积分 | POST /api/points/adjust |
| `getRewards` | 获取奖品列表 | GET /api/rewards |
| `createReward` | 创建奖品 | POST /api/rewards |
| `updateReward` | 更新奖品 | PUT /api/rewards/:id |
| `deleteReward` | 删除奖品 | DELETE /api/rewards/:id |
| `exchangeReward` | 兑换奖品 | POST /api/reward-records |
| `getRewardRecords` | 查询兑换记录 | GET /api/reward-records |

### 创建云函数的步骤

#### 方法1：在开发者工具中创建（推荐）

1. **右键点击 `cloudfunctions` 目录**
2. **新建 Node.js 云函数**
3. **输入函数名**（如 `login`）
4. **编辑 `index.js`**

#### 方法2：批量创建（快速）

我会为您准备所有云函数的代码，您只需：
1. 复制代码到对应的云函数目录
2. 右键上传部署

---

## 📝 云函数代码示例

### 示例1：login 云函数

```javascript
// cloudfunctions/login/index.js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()

exports.main = async (event, context) => {
  const { code } = event
  
  try {
    // 1. 使用code换取openid
    const wxContext = cloud.getWXContext()
    const openid = wxContext.OPENID
    
    // 2. 查询或创建用户
    const usersCollection = db.collection('users')
    let user = await usersCollection.where({ openid }).get()
    
    if (user.data.length === 0) {
      // 创建新用户
      const result = await usersCollection.add({
        data: {
          openid,
          role: 'child',
          nickname: '小红花宝宝',
          avatar_url: '',
          created_at: new Date(),
          updated_at: new Date()
        }
      })
      
      // 获取创建的用户
      user = await usersCollection.doc(result._id).get()
      return {
        code: 200,
        message: '登录成功',
        data: user.data
      }
    }
    
    return {
      code: 200,
      message: '登录成功',
      data: user.data[0]
    }
    
  } catch (error) {
    console.error('登录失败', error)
    return {
      code: 500,
      message: '登录失败',
      error: error.message
    }
  }
}
```

### 示例2：getTasks 云函数

```javascript
// cloudfunctions/getTasks/index.js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()

exports.main = async (event, context) => {
  try {
    const tasks = await db.collection('tasks')
      .where({
        is_active: true
      })
      .orderBy('created_at', 'desc')
      .get()
    
    return {
      code: 200,
      message: '操作成功',
      data: tasks.data
    }
    
  } catch (error) {
    console.error('获取任务失败', error)
    return {
      code: 500,
      message: '获取任务失败',
      error: error.message
    }
  }
}
```

### 示例3：completeTask 云函数

```javascript
// cloudfunctions/completeTask/index.js
const cloud = require('wx-server-sdk')
cloud.init({ env: cloud.DYNAMIC_CURRENT_ENV })
const db = cloud.database()
const _ = db.command

exports.main = async (event, context) => {
  const { recordId } = event
  
  try {
    // 1. 更新任务记录状态
    const record = await db.collection('task_records')
      .doc(recordId)
      .get()
    
    if (!record.data) {
      return { code: 404, message: '任务记录不存在' }
    }
    
    await db.collection('task_records')
      .doc(recordId)
      .update({
        data: {
          status: 'completed',
          completed_at: new Date(),
          updated_at: new Date()
        }
      })
    
    // 2. 获取任务信息
    const task = await db.collection('tasks')
      .doc(record.data.task_id)
      .get()
    
    // 3. 增加积分
    const reward = task.data.reward || 0
    
    // 添加积分历史记录
    await db.collection('point_history').add({
      data: {
        child_id: record.data.child_id,
        change: reward,
        balance: 0, // 需要计算
        reason: `完成任务: ${task.data.title}`,
        source_type: 'task',
        source_id: task.data._id,
        created_at: new Date()
      }
    })
    
    return {
      code: 200,
      message: '完成任务成功',
      data: {
        ...record.data,
        status: 'completed',
        reward: reward
      }
    }
    
  } catch (error) {
    console.error('完成任务失败', error)
    return {
      code: 500,
      message: '完成任务失败',
      error: error.message
    }
  }
}
```

---

## 🔧 第四步：修改小程序代码

### 1. 初始化云开发

修改 `miniprogram/app.js`：

```javascript
App({
  onLaunch() {
    // 初始化云开发
    wx.cloud.init({
      env: 'xiaohonghua-7gxxx', // 替换为您的环境ID
      traceUser: true
    })
    
    this.initApp()
  },
  
  async initApp() {
    // 登录
    await this.login()
    // 其他初始化...
  },
  
  async login() {
    try {
      const res = await wx.cloud.callFunction({
        name: 'login',
        data: {}
      })
      
      if (res.result.code === 200) {
        const user = res.result.data
        this.globalData.currentUser = user
        wx.setStorageSync('currentUser', user)
      }
    } catch (error) {
      console.error('登录失败', error)
    }
  },
  
  globalData: {
    currentUser: null
  }
})
```

### 2. 创建云函数调用封装

创建 `miniprogram/utils/cloudApi.js`：

```javascript
/**
 * 云函数API封装
 */

// 调用云函数的通用方法
async function callFunction(name, data = {}) {
  try {
    wx.showLoading({ title: '加载中...', mask: true })
    
    const res = await wx.cloud.callFunction({
      name,
      data
    })
    
    wx.hideLoading()
    
    if (res.result.code !== 200) {
      wx.showToast({
        title: res.result.message || '操作失败',
        icon: 'none'
      })
      return null
    }
    
    return res.result.data
    
  } catch (error) {
    wx.hideLoading()
    console.error(`调用云函数 ${name} 失败`, error)
    wx.showToast({
      title: '网络错误',
      icon: 'none'
    })
    return null
  }
}

// 获取任务列表
export async function getTasks() {
  return await callFunction('getTasks')
}

// 领取任务
export async function receiveTask(childId, taskId) {
  return await callFunction('receiveTask', { childId, taskId })
}

// 完成任务
export async function completeTask(recordId) {
  return await callFunction('completeTask', { recordId })
}

// 获取任务记录
export async function getTaskRecords(childId, status) {
  return await callFunction('getTaskRecords', { childId, status })
}

// 获取当前积分
export async function getCurrentPoints(childId) {
  return await callFunction('getCurrentPoints', { childId })
}

// 获取积分历史
export async function getPointHistory(childId) {
  return await callFunction('getPointHistory', { childId })
}

// 获取奖品列表
export async function getRewards() {
  return await callFunction('getRewards')
}

// 兑换奖品
export async function exchangeReward(childId, rewardId) {
  return await callFunction('exchangeReward', { childId, rewardId })
}

// 创建任务（家长端）
export async function createTask(task) {
  return await callFunction('createTask', task)
}

// 更新任务（家长端）
export async function updateTask(taskId, task) {
  return await callFunction('updateTask', { taskId, ...task })
}

// 删除任务（家长端）
export async function deleteTask(taskId) {
  return await callFunction('deleteTask', { taskId })
}

// 创建奖品（家长端）
export async function createReward(reward) {
  return await callFunction('createReward', reward)
}

// 更新奖品（家长端）
export async function updateReward(rewardId, reward) {
  return await callFunction('updateReward', { rewardId, ...reward })
}

// 删除奖品（家长端）
export async function deleteReward(rewardId) {
  return await callFunction('deleteReward', { rewardId })
}

// 手动调整积分（家长端）
export async function adjustPoints(childId, change, reason) {
  return await callFunction('adjustPoints', { childId, change, reason })
}
```

### 3. 修改页面调用方式

**之前（调用HTTP API）：**
```javascript
import * as api from '../../utils/realApi.js'

// 获取任务列表
const tasks = await api.getTasks()
```

**之后（调用云函数）：**
```javascript
import * as cloudApi from '../../utils/cloudApi.js'

// 获取任务列表
const tasks = await cloudApi.getTasks()
```

**主要改动的文件：**
- `pages/index/index.js` - 任务列表页
- `pages/rewards/rewards.js` - 奖品页
- `pages/wishes/wishes.js` - 许愿瓶页
- `pages/parent/` 下的所有家长端页面

---

## ✅ 第五步：测试验证

### 1. 本地测试

1. **编译小程序**
2. **测试登录功能**
3. **测试任务、积分、奖品功能**

### 2. 真机测试

1. **点击"真机调试"**
2. **扫码在手机上测试**

### 3. 检查云函数日志

在云开发控制台 → 云函数 → 日志，查看调用情况

---

## 📊 迁移进度检查清单

### 云开发环境
- [ ] 开通云开发环境
- [ ] 获取环境ID
- [ ] 初始化云数据库

### 云函数创建
- [ ] login（登录）
- [ ] getTasks（获取任务）
- [ ] receiveTask（领取任务）
- [ ] completeTask（完成任务）
- [ ] getTaskRecords（任务记录）
- [ ] getCurrentPoints（当前积分）
- [ ] getPointHistory（积分历史）
- [ ] getRewards（奖品列表）
- [ ] exchangeReward（兑换奖品）
- [ ] createTask（创建任务 - 家长）
- [ ] updateTask（更新任务 - 家长）
- [ ] deleteTask（删除任务 - 家长）
- [ ] createReward（创建奖品 - 家长）
- [ ] updateReward（更新奖品 - 家长）
- [ ] deleteReward（删除奖品 - 家长）
- [ ] adjustPoints（调整积分 - 家长）

### 小程序代码
- [ ] 初始化云开发
- [ ] 创建 cloudApi.js 封装
- [ ] 修改 index/index.js
- [ ] 修改 rewards/rewards.js
- [ ] 修改 wishes/wishes.js
- [ ] 修改家长端页面

### 测试
- [ ] 本地测试登录
- [ ] 测试任务功能
- [ ] 测试积分功能
- [ ] 测试奖品功能
- [ ] 真机测试
- [ ] 检查云函数日志

---

## 💰 云开发费用说明

### 免费额度（每月）

| 资源 | 免费额度 |
|-----|---------|
| 云函数调用次数 | 10万次 |
| 云函数资源使用量 | 4万GBs |
| 云数据库容量 | 2GB |
| 云数据库读操作 | 5万次 |
| 云数据库写操作 | 3万次 |
| 云存储容量 | 5GB |
| CDN流量 | 5GB |

**对于小红花初期用户量，完全在免费额度内！**

---

## 🎯 预计时间安排

| 任务 | 预计时间 |
|-----|---------|
| 开通云开发 | 10分钟 |
| 初始化数据库 | 20分钟 |
| 创建云函数 | 2小时 |
| 修改小程序代码 | 1小时 |
| 测试验证 | 30分钟 |
| **总计** | **3-4小时** |

---

## 🚀 开始迁移

**现在就可以开始第一步：开通云开发！**

打开微信开发者工具，点击"云开发"按钮，让我们开始吧！

完成后告诉我环境ID，我会为您准备所有云函数代码！🌸

